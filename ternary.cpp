#include <iostream>
using namespace std;

int main()
{
    int i = 1, j = 2;
    // ternary operator checking for whether i is greater than j. ouputs the greater number
    cout << (i > j ? i : j) << " is greater\n";
}